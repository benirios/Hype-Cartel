CLI Anything: Mixxx agent-harness

This package implements a CLI harness for Mixxx (initial skeleton).

Commands:
  - cli-anything-mixxx library list [--limit N]

Development notes:
  - Uses MIXxx DB at ~/.mixxx/mixxxdb.sqlite or MIXXX_DB_PATH env var
  - REPL available when running without subcommands
